{% extends 'base.html.twig' %}

{% block body %}
          <!-- header!-->
            <div class="parallax-container center valign-wrapper">
                <div class="container">
                    <div class="row">
                        <div class="col s12 white-text">
                              <img class="responsive-img" src="{{ asset('build/images/Logo.f5a4542a.png') }}" alt="Logo">
                        </div>
                        <div class="col s12">
                            <a href="#propertyContainer" id="menu" class="waves-effect waves-light btn btn-floating cyan lighten-3 pulse" onclick="$('.tap-target').tapTarget('close');"></a>
                        </div>
                    </div>
                </div>

                <div class="parallax"><img class="responsive-img" src="{{ asset('build/images/MainPageBack2.8886b5e7.png') }}" alt=""></div>
                <div class="parallax"><img class="responsive-img" src="{{ asset('build/images/MainPageBack.23a3055d.png') }}" alt=""></div>

            </div>
         <!-- header!-->
        <!--!-->
        <!-- feature discovery!-->

        <div class="tap-target grey darken-4" data-target="menu">
            <div class="tap-target-content">
                <h5>Explore Properties</h5 >
                <p>Click the icon</p>
            </div>
        </div>
        <!-- feature discovery!-->

        <!-- properties!-->
                <div class="section">
                    <div class="row">
                        {{ render(controller('App\\Controller\\PropertiesController::getFilterForm')) }}
                    </div>
                </div>
                <div id="propertyContainer" class="section scrollspy">

                </div>
        <!-- properties!-->

{% endblock %}


{% block javascripts %}
    {{ parent() }}

{% endblock %}