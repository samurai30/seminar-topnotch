{#

{% for prop in properties %}
     <tbody>
     <tr>
          <th scope="row">{{ prop.type }}</th>
          {% for scapeProp in prop.scapeProperty %}
               <th scope="row">{{ scapeProp.propName }}</th>
          {% endfor %}
     <tr>
     <tbody>
{% endfor %}#}

{{ form(form) }}